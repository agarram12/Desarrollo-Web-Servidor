let numeros = [2, 6, 7, 8];
numeros.find(6);
numeros.find(10);
numeros.indexOf(6);
numeros.indexOf(10);
// en find nos dirá si existe o no el número que estamos buscando y si lo contiene
// en indexOf nos devuelve la posición del indice en la que se encuentra el array